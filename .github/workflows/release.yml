name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Linux x86_64
            runner: ubuntu-24.04
            artifact: bloom-linux-x86_64.tar.gz

          - name: macOS
            runner: macos-14
            artifact: bloom-macos-arm64.dmg

          - name: Windows x86_64
            runner: windows-latest
            artifact: bloom-windows-x86_64.zip
            msi: bloom-windows-x86_64.msi

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Install dependencies (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config libheif-dev libdav1d-dev clang libclang-dev
          curl -fL -o /tmp/ffmpeg.tar.xz \
            https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n8.0-latest-linux64-gpl-shared.tar.xz
          mkdir -p "$HOME/ffmpeg"
          tar -xf /tmp/ffmpeg.tar.xz -C "$HOME/ffmpeg" --strip-components=1
          sed -i "s|^prefix=.*|prefix=$HOME/ffmpeg|" "$HOME/ffmpeg/lib/pkgconfig/"*.pc
          echo "FFMPEG_DIR=$HOME/ffmpeg" >> "$GITHUB_ENV"
          echo "PKG_CONFIG_PATH=$HOME/ffmpeg/lib/pkgconfig" >> "$GITHUB_ENV"
          echo 'RUSTFLAGS=-C link-arg=-Wl,-rpath,$ORIGIN/lib -C link-arg=-Wl,--disable-new-dtags' >> "$GITHUB_ENV"

      - name: Install dependencies (macOS)
        if: runner.os == 'macOS'
        run: brew install libheif dav1d ffmpeg

      - name: Cache vcpkg packages (Windows)
        if: runner.os == 'Windows'
        uses: actions/cache@v4
        with:
          path: C:\vcpkg\installed
          key: vcpkg-x64-windows-static-md-${{ hashFiles('.github/workflows/release.yml') }}
          restore-keys: vcpkg-x64-windows-static-md-

      - name: Install dependencies (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          VCPKG_MAX_CONCURRENCY: 2
        run: |
          choco install pkgconfiglite -y
          vcpkg integrate install
          vcpkg install libheif:x64-windows-static-md dav1d:x64-windows-static-md
          $vcpkgInstalled = "$env:VCPKG_INSTALLATION_ROOT\installed\x64-windows-static-md"
          echo "LIBHEIF_DIR=$vcpkgInstalled" >> $env:GITHUB_ENV
          echo "PKG_CONFIG_PATH=$vcpkgInstalled\lib\pkgconfig" >> $env:GITHUB_ENV
          echo "LIBCLANG_PATH=C:\Program Files\LLVM\bin" >> $env:GITHUB_ENV
          Invoke-WebRequest -Uri "https://github.com/BtbN/FFmpeg-Builds/releases/download/latest/ffmpeg-n8.0-latest-win64-gpl-shared.zip" -OutFile "$env:RUNNER_TEMP\ffmpeg.zip"
          Expand-Archive -Path "$env:RUNNER_TEMP\ffmpeg.zip" -DestinationPath "$env:RUNNER_TEMP"
          $extracted = (Get-ChildItem -Directory "$env:RUNNER_TEMP" -Filter "ffmpeg-n8.0*").FullName
          echo "FFMPEG_DIR=$extracted" >> $env:GITHUB_ENV

      - name: Set macOS deployment target
        if: runner.os == 'macOS'
        run: echo "MACOSX_DEPLOYMENT_TARGET=10.15" >> $GITHUB_ENV

      - name: Build
        run: cargo build --release --features heif,video

      - name: Package (Linux)
        if: runner.os == 'Linux'
        run: |
          mkdir -p dist/lib
          cp target/release/bloom dist/
          cp -a "$FFMPEG_DIR"/lib/*.so* dist/lib/
          tar -czf bloom-linux-x86_64.tar.gz -C dist .

      - name: Package (macOS)
        if: runner.os == 'macOS'
        shell: bash
        run: |
          APP="bloom.app"
          BINARY="$APP/Contents/MacOS/bloom"
          FRAMEWORKS="$APP/Contents/Frameworks"
          BREW_PREFIX="$(brew --prefix)"

          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            VERSION="${GITHUB_REF_NAME#v}"
          else
            VERSION="0.0.0"
          fi

          mkdir -p "$APP/Contents/MacOS" "$FRAMEWORKS"
          cp target/release/bloom "$BINARY"

          cat > "$APP/Contents/Info.plist" <<EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>CFBundleExecutable</key><string>bloom</string>
            <key>CFBundleIdentifier</key><string>com.bloom.app</string>
            <key>CFBundleName</key><string>Bloom</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>CFBundleShortVersionString</key><string>$VERSION</string>
            <key>NSHighResolutionCapable</key><true/>
            <key>CFBundleDocumentTypes</key>
            <array>
              <dict>
                <key>CFBundleTypeName</key><string>Image</string>
                <key>CFBundleTypeRole</key><string>Viewer</string>
                <key>LSHandlerRank</key><string>Alternate</string>
                <key>LSItemContentTypes</key>
                <array>
                  <string>public.jpeg</string>
                  <string>public.png</string>
                  <string>com.compuserve.gif</string>
                  <string>com.microsoft.bmp</string>
                  <string>public.tiff</string>
                  <string>public.svg-image</string>
                  <string>com.adobe.photoshop</string>
                  <string>public.webp</string>
                </array>
                <key>CFBundleTypeExtensions</key>
                <array>
                  <string>jpg</string><string>jpeg</string>
                  <string>png</string><string>gif</string>
                  <string>bmp</string><string>tif</string><string>tiff</string>
                  <string>webp</string><string>svg</string><string>svgz</string>
                  <string>psd</string><string>ico</string><string>tga</string>
                  <string>dds</string>
                </array>
              </dict>
              <dict>
                <key>CFBundleTypeName</key><string>Image</string>
                <key>CFBundleTypeRole</key><string>Viewer</string>
                <key>LSHandlerRank</key><string>Default</string>
                <key>LSItemContentTypes</key>
                <array>
                  <string>public.heic</string>
                  <string>public.heif</string>
                  <string>public.avif</string>
                </array>
                <key>CFBundleTypeExtensions</key>
                <array>
                  <string>heic</string><string>heif</string>
                  <string>avif</string><string>jxl</string>
                  <string>qoi</string><string>hdr</string><string>exr</string>
                </array>
              </dict>
            </array>
          </dict>
          </plist>
          EOF

          bundle_dylib() {
            local lib="$1"
            local name
            name="$(basename "$lib")"
            [ -f "$FRAMEWORKS/$name" ] && return
            cp "$lib" "$FRAMEWORKS/$name"
            while IFS= read -r dep; do
              install_name_tool -change "$dep" "@executable_path/../Frameworks/$(basename "$dep")" "$FRAMEWORKS/$name"
              bundle_dylib "$dep"
            done < <(otool -L "$FRAMEWORKS/$name" | grep "$BREW_PREFIX" | awk '{print $1}')
          }

          while IFS= read -r lib; do
            install_name_tool -change "$lib" "@executable_path/../Frameworks/$(basename "$lib")" "$BINARY"
            bundle_dylib "$lib"
          done < <(otool -L "$BINARY" | grep "$BREW_PREFIX" | awk '{print $1}')

          shopt -s nullglob
          for dylib in "$FRAMEWORKS"/*.dylib; do
            codesign -f -s "-" "$dylib"
          done
          codesign -f -s "-" "$BINARY"

          hdiutil create -volname "Bloom" -srcfolder "$APP" -ov -format UDZO "${{ matrix.artifact }}"

      - uses: taiki-e/install-action@v2
        if: runner.os == 'Windows'
        with:
          tool: cargo-wix

      - name: Package (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $dllDir = "$env:RUNNER_TEMP\ffmpeg_dlls"
          New-Item -ItemType Directory -Force -Path $dllDir | Out-Null
          Copy-Item "$env:FFMPEG_DIR\bin\*.dll" -Destination $dllDir
          & "$env:WIX\bin\heat.exe" dir "$dllDir" -nologo -cg FfmpegDlls -dr Bin -var var.FfmpegDllDir -ag -srd -sreg -scom -out wix\ffmpeg.wxs
          cargo wix --no-build --nocapture -C "-dFfmpegDllDir=$dllDir" --output bloom-windows-x86_64.msi wix\main.wxs wix\ffmpeg.wxs
          $zipItems = @("target\release\bloom.exe") + (Get-ChildItem "$dllDir\*.dll").FullName
          Compress-Archive -Path $zipItems -DestinationPath bloom-windows-x86_64.zip

      - uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.artifact }}

      - uses: actions/upload-artifact@v4
        if: runner.os == 'Windows'
        with:
          name: ${{ matrix.msi }}
          path: ${{ matrix.msi }}

  publish:
    name: Publish
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            bloom-*.tar.gz
            bloom-*.dmg
            bloom-*.zip
            bloom-*.msi
